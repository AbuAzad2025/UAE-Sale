# .gitleaksignore
#
# Valid entries are full fingerprints:  <commit>:<file>:<rule>:<line>
# Legacy bare-filename entries (nowpayments_config.py, env.pythonanywhere,
# deploy_dump.sql, check_ai_paths.py, trigger_backup.py) were removed: those
# files were deleted or no longer contain credentials, and the working tree
# scans clean.  If a genuine legacy secret resurfaces in history, add its
# exact fingerprint below (after rotating the credential out of production).
